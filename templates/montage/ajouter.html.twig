{% extends 'base.html.twig' %}

{% block title %} Ajouter Montage {% endblock %}

{% block body %}
    <div class="container">
        <h2 class="text-center">Ajouter Montage</h2><br>

        {{ form_start(f, {'attr': {'class': 'form-horizontal'}}) }}
        {{ form_errors(f) }}

        <div class="form-group">
            {{ form_label(f.nomMontage, 'Nom Montage', {'label_attr': {'class': 'col-sm-2 control-label text-primary'}}) }}
            <div class="col-sm-10">
                {{ form_widget(f.nomMontage, {'attr': {'class': 'form-control', 'style': 'border-color: #3498db;'}}) }}
                {{ form_errors(f.nomMontage) }}
            </div>
        </div>

        <div class="form-group">
            {{ form_label(f.client, 'Client', {'label_attr': {'class': 'col-sm-2 control-label text-success'}}) }}
            <div class="col-sm-10">
                {{ form_widget(f.client, {'attr': {'class': 'form-control', 'style': 'border-color: #2ecc71;'}}) }}
                {{ form_errors(f.client) }}
            </div>
        </div>

        <div class="form-group">
            {{ form_label(f.created_At, 'Date de création', {'label_attr': {'class': 'col-sm-2 control-label text-warning'}}) }}
            <div class="col-sm-10">
                {{ form_widget(f.created_At, {'attr': {'class': 'form-control', 'style': 'border-color: #f39c12;'}}) }}
                {{ form_errors(f.created_At) }}
            </div>
        </div>

        <div class="form-group">
            {{ form_label(f.cout, 'Cout', {'label_attr': {'class': 'col-sm-2 control-label text-danger'}}) }}
            <div class="col-sm-10">
                {{ form_widget(f.cout, {'attr': {'class': 'form-control', 'style': 'border-color: #e74c3c;'}}) }}
                {{ form_errors(f.cout) }}
            </div>
        </div>

        <div class="form-group">
            {{ form_label(f.image, 'Image', {'label_attr': {'class': 'col-sm-2 control-label text-info'}}) }}
            <div class="col-sm-10">
                {{ form_widget(f.image, {'attr': {'class': 'form-control', 'style': 'border-color: #3498db;'}}) }}
                {{ form_errors(f.image) }}
            </div>
        </div>

     <div class="form-group">
    <div class="col-sm-offset-2 col-sm-10">
        {{ form_widget(f.valider, {'attr': {'class': 'btn btn-primary'}}) }}
    </div>
</div>

        {{ form_end(f) }}
    </div>
{% endblock %}
